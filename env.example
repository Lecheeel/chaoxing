# 超星学习通自动签到系统 - 环境配置文件
# 复制此文件为 .env 并根据需要修改配置

# Web服务端口 (默认: 5000)
# 如果端口被占用，系统会自动查找可用端口
PORT=5000

# 启动时是否自动打开浏览器 (默认: true)
# 设置为 false 将不会自动打开浏览器
OPEN_BROWSER=true

# Flask应用调试模式 (默认: false)
# 生产环境请设置为 false
DEBUG=false

# Flask应用密钥 (用于会话管理)
# 生产环境请修改为随机字符串
SECRET_KEY=chaoxing-auto-sign-secret-key

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
# 生产环境建议使用 INFO 或 WARNING
LOG_LEVEL=INFO

# 守护进程检查间隔 (秒) (默认: 10)
DAEMON_CHECK_INTERVAL=10

# 守护进程最大重启次数 (每天) (默认: 5)
DAEMON_MAX_RESTART=5
